
<!-- application_helper의 full_title로 전달 -->
<% provide(:title, 'Sign up') %>
<% provide(:url, signup_path) %>
<% provide(:button_text, 'Create my account') %>
<!--layout으로 파라미터를 전달함. 레이아웃에서는 yield함수로 받을 수 있음.-->
<h1>Sign up</h1>

<div class="row">
  <div class="col-md-6 offset-md-3">
    <%= render 'form' %>
  </div>
</div>
